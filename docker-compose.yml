version: '3.7'

services:
  main:
    build:
      context: .
      target: build
    volumes:
      - ./:/app
      - /usr/src/app/node_modules
    ports:
      - 1002:1002
    networks:
      - main-application
    command: yarn start:dev

networks:
  main-application:
    driver: bridge
